# nidara-system — do not arm systemd's reboot/shutdown watchdog.
#
# ⚠️ INHERITED, NOT DECIDED — same provenance and same caveat as
# /etc/modprobe.d/nidara-nowatchdog.conf.
[Manager]
RuntimeWatchdogSec=off
RebootWatchdogSec=off
ShutdownWatchdogSec=off
KExecWatchdogSec=off
